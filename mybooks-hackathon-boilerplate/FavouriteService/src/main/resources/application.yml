spring:
  application:
    name: FavouriteService
  data:
    mongodb:
      #      for hobbes and gitlab ci please uncomment the username and password
      #      username: ${MONGO_USER}
      #      password: ${MONGO_PASSWORD}
      database: ${MONGO_DATABASE:mybook_favouritedb}
      port: ${MONGO_PORT:27017}
      host: ${MONGO_HOST:localhost}

server:
  port: 8083